= render 'nav'

- if @requests.empty?
  .alert.alert-info
    = t '.empty'
- else
  %ul.requests
    - @requests.each do |request|
      = render partial: 'request', locals: { request: request }
    %li
      = page_entries_info @requests
  .pull-right
    = paginate @requests
