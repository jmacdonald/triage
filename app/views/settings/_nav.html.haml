- content_for :nav do
  .well.context-menu
    %ul.nav.nav-list
      %li.nav-header
        = t 'nav.settings'
      %li{ class: ('active' if (params[:controller] == 'settings' and params[:action] == 'edit_profile') ) }
        = link_to edit_profile_path do
          = t 'nav.profile'
